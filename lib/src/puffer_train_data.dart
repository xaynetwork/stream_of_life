import 'package:stream_of_life/src/cell.dart';

final dataSet = {
  // left shape large
  const Cell(x: -11, y: -3),
  const Cell(x: -10, y: -3),
  const Cell(x: -9, y: -3),
  const Cell(x: -8, y: -2),
  const Cell(x: -11, y: -2),
  const Cell(x: -11, y: -1),
  const Cell(x: -11, y: 0),
  const Cell(x: -10, y: 1),
  const Cell(x: -8, y: 1),
  // left shape small
  const Cell(x: -4, y: -1),
  const Cell(x: -3, y: -1),
  const Cell(x: -2, y: -1),
  const Cell(x: -2, y: 0),
  const Cell(x: -2, y: 1),
  const Cell(x: -5, y: 0),
  const Cell(x: -5, y: 1),
  const Cell(x: -4, y: 1),
  // right shape small
  const Cell(x: 2, y: -1),
  const Cell(x: 3, y: -1),
  const Cell(x: 4, y: -1),
  const Cell(x: 2, y: 0),
  const Cell(x: 2, y: 1),
  const Cell(x: 5, y: 0),
  const Cell(x: 5, y: 1),
  const Cell(x: 4, y: 1),
  // right shape large
  const Cell(x: 11, y: -3),
  const Cell(x: 10, y: -3),
  const Cell(x: 9, y: -3),
  const Cell(x: 8, y: -2),
  const Cell(x: 11, y: -2),
  const Cell(x: 11, y: -1),
  const Cell(x: 11, y: 0),
  const Cell(x: 10, y: 1),
  const Cell(x: 8, y: 1),
};
